# Set default behavior, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always be
# normalized and converted to native line endings on checkout.
*.c text
*.for text
*.h text
*.R text
*.Rd text
*.Rnw text
*.sh text
*.msa text
*.py text
*.plt text
Makefile text


# Declare files that will always have CRLF line endings on checkout.
# DM scripts
*.s text eol=crlf
# Batch cmd scripts
*.cmd text eol=crlf
# VS soln files
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
# .rda and .RData are gzipped binary files
*.RData binary
*.rda binary
*.png binary
*.jpg binary
*.pdf binary

